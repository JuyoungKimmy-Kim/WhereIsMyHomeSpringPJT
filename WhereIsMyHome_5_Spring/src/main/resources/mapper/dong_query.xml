<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycom.myapp.code.dao.DongDao"> <!--  @Mapper annotation 있는 dao -->

	<!-- 구군 코드로 동 리스트 받아오기 -->
	<select id="dongList" resultType="com.mycom.myapp.code.dto.DongDto">
		select code, name, city_code, city_name, gugun_code, gugun_name
		  from dong_code
		 where gugun_code= #{gugunCode}
	</select>

</mapper>
