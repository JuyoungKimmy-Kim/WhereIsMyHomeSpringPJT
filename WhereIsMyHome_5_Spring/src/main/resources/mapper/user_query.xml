<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycom.myapp.user.dao.UserDao"> <!--  @Mapper annotation 있는 dao -->

<insert id="joinUser" parameterType="com.mycom.myapp.user.dto.UserDto">
	insert into users(user_name, user_password, user_email, user_register_date, user_clsf)
		values (#{userName},#{userPassword}, #{userEmail}, now(), #{userClsf})
</insert>

<select id="getCodeclsf" resultType="com.mycom.myapp.user.dto.CodeDto">
	select group_code, code, code_name
	from code
</select>

<select id="login" parameterType="string" resultType="com.mycom.myapp.user.dto.UserDto">
	select user_seq, user_name, user_password, user_email, user_profile_image_url, user_register_date, user_clsf
	from users
	where user_email = #{userEmail}
</select>

<update id="updateUser" parameterType="com.mycom.myapp.user.dto.UserDto">
	update users
	set user_name=#{userName},
		user_email=#{userEmail},
		user_password=#{userPassword}
	where user_seq = #{userSeq}
</update>

<delete id="deleteUser" parameterType="int">
	delete from users
	where user_seq=#{userSeq}
</delete>
</mapper>
