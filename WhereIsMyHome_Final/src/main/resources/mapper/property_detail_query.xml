<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycom.myhome.address.PropertyDetailDao"> <!--  @Mapper annotation 있는 dao -->
	<!-- 동 이름으로 집 정보 받아오기-->
	<select id="propertyDetail" resultType="com.mycom.myhome.address.PropertyDetailDto">
		select no, dong, AptName, code, dealAmount, buildYear, dealYear, dealMonth, dealDay,
			   area, floor, jibun, house_no
		  from housedeal
		 where house_no=#{houseNo}
	</select>
</mapper>	